<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.caitlin.tournamentmanager">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Controller.LoginScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controller.SignUpActivity" />
        <activity android:name=".Controller.MainMenuActivity" />
        <activity android:name=".Controller.RuleBookViewActivity" />
        <activity
            android:name=".Controller.RosterViewActivity"
            android:label="@string/title_activity_roster_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Controller.AddPlayerActivity" />
        <activity android:name=".Controller.CreateTournamentActivity" />
        <activity android:name=".Controller.ViewAllTeamsActivity" />
        <activity android:name=".Controller.MyTournaments" />
        <activity android:name=".Controller.MyTournamentsActivity" />
        <activity android:name=".Controller.TournamentChatActivity" />
        <activity android:name=".Controller.EditMyTeamActivity" />
        <activity android:name=".Controller.RemovePlayerActivity" />
        <activity android:name=".Controller.OtherTeamRosterView"></activity>
    </application>

</manifest>